<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/white">

        <LinearLayout
            android:id="@+id/lnr_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:gravity="center"
            android:padding="15dp"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/iv_team_icon"
                android:layout_width="35dp"
                android:layout_height="35dp"
                android:src="@color/cardview_dark_background"
                android:paddingBottom="5dp" />

            <TextView
                android:id="@+id/tv_team"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Team"
                android:textColor="@color/black"
                app:layout_constraintTop_toBottomOf="@id/iv_team_icon"
                app:layout_constraintStart_toStartOf="@id/iv_team_icon"
                app:layout_constraintEnd_toEndOf="@id/iv_team_icon" />
        </LinearLayout>

        <View
            android:layout_width="1dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toTopOf="@id/lnr_container"
            app:layout_constraintBottom_toBottomOf="@id/lnr_container"
            app:layout_constraintEnd_toEndOf="@id/lnr_container"
            android:background="@color/black" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>